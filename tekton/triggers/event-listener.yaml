apiVersion: triggers.tekton.dev/v1beta1
kind: EventListener
metadata:
  name: sock-shop-ci-listener
  namespace: microservice-app
spec:
  serviceAccountName: pipeline
  triggers:
    - name: github-push
      bindings:
        - ref: sock-shop-github-push
      template:
        ref: sock-shop-trigger-template
